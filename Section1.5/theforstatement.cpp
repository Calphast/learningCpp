#include <iostream>

int main() {
    // Exercise 1.13
    // int nums = 50;

    // // while (counter < 50)
    // // {
    // //     nums += counter;
    // //     ++counter;
    // // }
    
    // for(int i = 0; i < 50; ++i) {
    //     nums += i;
    // } 

    // std::cout << "the result: " << nums << std::endl;

    // int counter = 10;

    // while (counter > 0)
    // {
    //     std::cout << counter << std::endl;
    //     --counter;
    // }

    for(int i = 10; i > 0; --i) {
        std::cout << i << std::endl;
    }

    return 0;
}